# Variáveis usadas pelo docker-compose.yml para subir o PostgreSQL local.
# As variáveis específicas do front-end e do back-end ficam em
# frontend/.env.example e backend/.env.example.

POSTGRES_USER=pulse
POSTGRES_PASSWORD=pulse
POSTGRES_DB=pulse
POSTGRES_PORT=5432
